// let a = [1, 12, 3, 4, 5, 7]

// console.log(a)

// console.log(a.join("-"));

// // console.log(a.toString());

// console.log(a.pop()) //using console.log() -- this gives or returns the value; Although the method itself can be used or implemented without using console.log()
// console.log(a)

// console.log(a.push(88)) // doing so, this will return the new length of an array; Although the method itself can be used or implemented without using console.log()
// console.log(a)

// a.splice(2,1,22,24,43);
// console.log(a); // this will remove the elements and add new elements in place of them

// a.reverse();
// console.log(a); //this will reverse the array


// console.log(a.map((value)=>{
//     return value*value;
// })); // this is used to perform the operations on each element and creates a new array of the output of the operations

// const lessThan11=e=>{
//     e<11?console.log(e):false;
// };
// console.log(a.filter(lessThan11)); // this is used to filter the array elements by given logic


// a.reduce(); --> used to reduce the array into a single value.


// const arr=[1,2,3,4,5,6];
/*
// Factorial of number
let fact = (n) =>{
    let result =1;
    for(i=1;i<=n;i++){
        result*=i;
    }
    return result;
};

console.log(fact(6));
*/

//Method to create an Array from 0 to n-->
//Array.from(Array(number).keys()) -- The 'number' is a placeholder for any integer number

//Can store it in a variable->
// let arr = Array.from(Array(6).keys()) //I've created an array from 0 to 6 and its output is arr[0,1,2,3,4,5]
// console.log(arr);

